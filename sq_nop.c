#include "monty.h"

/**
 * sq_nop - do nothing (free gift)
 * @stack: stack
 * @line_number: line number of interpreted opened file
 */
void sq_nop(stack_t **stack, unsigned int line_number)
{
(void) stack;
(void) line_number;
}
